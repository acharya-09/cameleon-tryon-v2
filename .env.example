# Get your API key from https://decart.ai
VITE_DECART_API_KEY=your_api_key_here

# RunPod API (server-side only — never exposed to browser)
RUNPOD_API_KEY=your_runpod_api_key
RUNPOD_ENDPOINT_ID=your_runpod_endpoint_id

# ImgBB API (server-side only — never exposed to browser)
IMGBB_API_KEY=your_imgbb_api_key
